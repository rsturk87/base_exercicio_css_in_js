import { HeroSection, HeroContainer, HeroTitle } from './Hero.styles'

const Hero = () => (
  <HeroSection>
    <HeroContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </HeroContainer>
  </HeroSection>
)

export default Hero
